// 1. Перепишите следующий код с использованием сокращенной формы:

    let test = true;

    if (!test) { // Изменено. Было: if (test == false) {
        console.log('+++');
    } else {
        console.log('---');
    }

// 2. Перепишите следующий код с использованием сокращенной формы:

    let test = true;

    if (!test) { // Изменено. Было: if (test != true) {
        console.log('+++');
    } else {
        console.log('---');
    }

// 3. Перепишите следующий код с использованием сокращенной формы:

    let test = true;

    if (test) { // Изменено. Было: if (test != false) {
        console.log('+++');
    } else {
        console.log('---');
    }