// 1. Пусть у нас есть переменная min, в которой хранится количество минут от 1 до 60:

    let min = 10;

// Определите, в какую треть часа попадает указанное количество минут.

    if (min >= 0 && min <= 20) {
        console.log('1 треть');
    }

    if (min >= 21 && min <= 40) {
        console.log('2 треть');
    }

    if (min >= 41 && min <= 60) {
        console.log('3 треть');
    }