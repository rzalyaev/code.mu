// 1. Перепишите следующий код через описанный подход:

    const arr = [1, 2, 3, 4, 5]; // Изменено. Было: "let arr = [1, 2, 3, 4, 5];"
    const res = arr[1] + arr[2]; // Изменено. Было: "let res = arr[1] + arr[2];"

    console.log(res);